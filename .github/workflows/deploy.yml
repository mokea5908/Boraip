name: Deploy to Server

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Build & Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install & Lint
        run: |
          npm ci
          npm run lint

      - name: Build
        run: npm run build

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          password: ${{ secrets.SERVER_PASSWORD }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            cd ~/boraip
            git pull origin main
            /usr/local/bin/docker build -t bora-ip:latest .
            /usr/local/bin/docker stop bora-ip || true
            /usr/local/bin/docker rm bora-ip || true
            /usr/local/bin/docker run -d \
              --name bora-ip \
              --restart always \
              -p 9054:80 \
              bora-ip:latest
